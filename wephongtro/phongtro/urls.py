from django.urls import path

from .views import (
    book_room,
    cancel_my_booking,
    create_room,
    home_page,
    public_home_page,
    login_user,
    login_view,
    logout_view,
    add_review,
    approve_booking,
    reject_booking,
    AdminDashboardView,
    AdminLandlordListView,
    AdminRoomListView,
    AdminTenantListView,
    RoomCreateView,
    RoomBookingDashboardView,
    RoomDashboardListView,
    RoomDeleteView,
    RoomUpdateView,
    TenantBookingListView,
    register_user,
    register_view,
    room_detail,
    profile_view,
)

app_name = "phongtro"

urlpatterns = [
    path("", home_page, name="home_page"),
    path("danh-sach-phong/", public_home_page, name="public_home_page"),
    path("profile/", profile_view, name="profile"),
    path("rooms/<int:room_id>/", room_detail, name="room_detail"),
    path("rooms/<int:room_id>/review/", add_review, name="add_review"),
    path("rooms/<int:room_id>/book/", book_room, name="book_room"),
    path("my-bookings/", TenantBookingListView.as_view(), name="my_bookings"),
    path("my-bookings/<int:booking_id>/cancel/", cancel_my_booking, name="cancel_my_booking"),
    path("login-user/", login_user, name="login_user"),
    path("login/", login_view, name="login"),
    path("register-user/", register_user, name="register_user"),
    path("register/", register_view, name="register"),
    path("logout/", logout_view, name="logout"),
    path("rooms/create/", create_room, name="create_room"),
    path("dashboard/", RoomDashboardListView.as_view(), name="dashboard"),
    path("system-admin/", AdminDashboardView.as_view(), name="admin_dashboard"),
    path("system-admin/landlords/", AdminLandlordListView.as_view(), name="admin_landlords"),
    path("system-admin/tenants/", AdminTenantListView.as_view(), name="admin_tenants"),
    path("system-admin/rooms/", AdminRoomListView.as_view(), name="admin_rooms"),
    path("dashboard/bookings/", RoomBookingDashboardView.as_view(), name="dashboard_bookings"),
    path("dashboard/bookings/<int:booking_id>/approve/", approve_booking, name="approve_booking"),
    path("dashboard/bookings/<int:booking_id>/reject/", reject_booking, name="reject_booking"),
    path("dashboard/add/", RoomCreateView.as_view(), name="dashboard_add"),
    path("dashboard/edit/<int:pk>/", RoomUpdateView.as_view(), name="dashboard_edit"),
    path("dashboard/delete/<int:pk>/", RoomDeleteView.as_view(), name="dashboard_delete"),
]
